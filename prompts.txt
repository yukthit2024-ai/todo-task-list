===================================================================================================
create an android application which has a screen with a list of TO-DO master list
Each TO-DO master item is a list of individual To-DO item.
The TO-DO master is a XML and has to be saved in user directory (configurable via settings page).

The TO-DO master (XML file) can to be shared with a friend via matrix protocol message.
Have matrix credentials to a new side menu item "Matrix Credentails".  It should contain only Home-Server, userid, password
Add new side menu "Groups/Rooms" - This should contain a list of Group_Name/Room_Id pairs.  This is a editable list
have back arrow/buttons on each Settings page

On "share" action by the user, a list of "Groups/Rooms" is shown to user and once the room is selected, the JSON data is sent to hat matrix group/room using matrix credentials

The app should also have an option to "Import TODO master" when a matrix message arrives to the user.  Use can accept or reject the TO-DO master list from a friend.

develop the app in java (not kotlin)
Need a left side menu with items like "Settings", "Help", "About", with their own pages
They should have back arrow on top to return to main menu.

The left side menu is covering from top to bottom.  Even when menu is visible, it should not cover the app top bar and so on....
use latest gradle version supported by github actions
include gradle properties and gradle wrapper dependencies in the project



Give it as a single prompt to give to an agent to achieve this



more prompts
-----------
In the master list add a '+' button at bottom right to add a master list.  This will be a name, followd by adding items to the master list.
in the master list, only show a list of master TODO.  no need to show child items
main screen should show master TODO list only,  Clicking on it, should open a new page with all child entries.
all master lists should be saved in file system at /sdcard/Vypeensoft/TODO_Task_LIst/master_lists/
each master list on main screen to be saved under their own json file under /sdcard/Vypeensoft/TODO_Task_LIst/master_lists/

This path /sdcard/Vypeensoft/TODO_Task_LIst/master_lists/ should be configurable via the settings page.  All settings to be saved under all master lists should be saved in file system at /sdcard/Vypeensoft/TODO_Task_LIst/settings/settings.json
On startup, app should if folder /sdcard/Vypeensoft/TODO_Task_LIst/master_lists/ doesnt exist, then create it.  Infact, the folder to be checked and created is from the settings
On startup, app should read the master jsons from /sdcard/Vypeensoft/TODO_Task_LIst/master_lists/*.json and display on the main screen...
Provide sane color theme, with good contrasts ie.. industry standard/acceptable
on the main list, provide a delete icon (garbage bin) to delete one master TODO.  It should soft delete the JSON.  ie... rename  it to a FILENAME+timestamp format and move it to an "deleted" folder under the master_list
on the sub list, have a checkbox at the end of the line to mark it as complete.  Once marked as complete, strike the item text.  store the competed status in the json as well
on app start,master list on screen is empty. When user goes to settings and save the folder path, then it is able to pickup the master TODO and populate the screen

Modify AboutActivity.java and related source code in this project according to code taken from X:\Projects_X\0_Active\8_Android_APK\Randomizer_APK_GITHUB\app\src\main\java\com\vypeensoft\randomizer\AboutActivity.java
Along with completion checkbox, also provide a delete icon (dustbin) for each item. This should delete the record from the underlying json file
--------------------------------------------------------------------------------------------
output


Here’s a clean, **single comprehensive prompt** you can give to an autonomous coding agent to build your Android app exactly as specified:

---

**PROMPT FOR AGENT**

Create a complete Android application in **Java (NOT Kotlin)** with the following requirements and architecture.

---

## 1. Core App Concept

Build a **TO-DO Master List Manager** where:

* A **TO-DO Master** is a collection of multiple TO-DO lists.
* Each **TO-DO list (master item)** contains multiple individual TO-DO items.

---

## 2. Data Model

* Represent the TO-DO master as an **XML file**.

* XML structure should be hierarchical:

  ```xml
  <todo_master>
      <todo_list name="Groceries">
          <item>Milk</item>
          <item>Bread</item>
      </todo_list>
  </todo_master>
  ```

* The XML file must:

  * Be saved locally
  * Path must be **user-configurable via Settings page**
  * Default location: app-specific storage directory

---

## 3. UI Requirements

### Main Screen

* Displays list of **TO-DO Master items (lists)**
* Clicking a list opens its items
* Options to:

  * Add/Edit/Delete lists
  * Add/Edit/Delete items inside list
  * Share TO-DO master

---

## 4. Navigation (Left Drawer Menu)

Implement a **left-side navigation drawer**:

* Covers full height (top to bottom)
* Must NOT overlap the top app bar (Toolbar remains visible)
* Use standard Android DrawerLayout

### Menu Items:

1. Main (TO-DO screen)
2. Settings
3. Matrix Credentials
4. Groups / Rooms
5. Help
6. About

Each page must:

* Have a **top app bar with back arrow**
* Back arrow returns to main screen

---

## 5. Settings Page

* Configure:

  * File storage directory (path input or picker)
* Persist using SharedPreferences

---

## 6. Matrix Credentials Page

Fields:

* Home Server
* User ID
* Password

Requirements:

* Store securely (SharedPreferences for now)
* Editable form
* Save button

---

## 7. Groups / Rooms Page

* Editable list of:

  * Group Name
  * Room ID
* Features:

  * Add new pair
  * Edit existing
  * Delete
* Persist locally (SharedPreferences or SQLite)

---

## 8. Share Functionality

When user clicks **Share**:

1. Show dialog listing **Groups/Rooms**
2. User selects one
3. Convert TO-DO master XML → JSON
4. Send JSON via **Matrix protocol message** using stored credentials

---

## 9. Matrix Integration

Implement basic Matrix client functionality:

* Login using:

  * Home server
  * User ID
  * Password
* Send message to a room:

  * Room ID from Groups list
  * Payload = JSON of TO-DO master

Use REST APIs of Matrix (no heavy SDK required unless necessary).

---

## 10. Import Feature

* Listen for incoming Matrix messages (polling or simple sync)
* When a TO-DO JSON is received:

  * Show prompt:

    * Accept
    * Reject
* If accepted:

  * Convert JSON → XML
  * Save as TO-DO master file (overwrite or new file)

---

## 11. Architecture

* Language: **Java**
* Use:

  * MVVM or simple MVC
  * RecyclerView for lists
  * Fragments for screens
* Networking:

  * Retrofit or HttpURLConnection
* XML parsing:

  * DOM or SAX parser

---

## 12. Project Setup

* Use **latest Gradle version compatible with GitHub Actions**
* Include:

  * `gradle-wrapper.properties`
  * `gradlew`, `gradlew.bat`
  * `gradle.properties`
* Ensure project builds via CLI:

  ```
  ./gradlew build
  ```

---

## 13. UI/UX Notes

* Clean Material Design
* Responsive layouts
* Toasts or dialogs for feedback
* Confirmation dialogs for destructive actions

---

## 14. Deliverables

Provide:

1. Full Android project structure
2. All Java source files
3. XML layouts
4. Gradle configs
5. README with:

   * Build instructions
   * Matrix setup notes

---

## 15. Constraints

* Must be **pure Java (no Kotlin)**
* Must be **fully runnable project**
* Avoid unnecessary libraries
* Code should be clean and modular

---

Build the complete working application satisfying all above requirements.

---

If you want, I can also generate a **starter project structure + key classes** to speed this up.
